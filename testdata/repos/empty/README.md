# Empty Repository

An empty repository for testing error handling.

This repository contains no language-specific files, no Dockerfile, and no flake.nix.
The detector should return an error with suggestions for manual configuration.
