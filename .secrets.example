# Secrets file template for local GitHub Actions testing with act
# Copy this file to .secrets and fill in your values
#
# Usage:
#   cp .secrets.example .secrets
#   # Edit .secrets with your values
#   act
#
# IMPORTANT: Never commit .secrets to version control!

# GitHub token for API access (optional, needed for some actions)
# Create a personal access token at: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token_here

# Database credentials (used by test job)
# These match the PostgreSQL service in ci.yml
DATABASE_URL=postgres://testuser:testpass@localhost:5432/testdb?sslmode=disable
TEST_DATABASE_URL=postgres://testuser:testpass@localhost:5432/testdb?sslmode=disable

# JWT secret for authentication tests
JWT_SECRET=test-secret-key-minimum-32-characters-long

# Release workflow secrets (only needed when testing release.yml)
# CHANGELOG_REPO_TOKEN=your_changelog_repo_token_here
